# Artifacts

Small, reproducible evaluation outputs may be stored here.

Model weights and training checkpoints belong in `artifacts/models/`, which is
ignored by Git because these files can exceed GitHub's size limit.
